import {
  FETCHING_TAGS,
  TAGS_FETCHED,
  ADDING_TAG,
  TAG_ADDED,
  TAG_ERROR
} from '../constants/actionTypes';

const initialState = {

}

export const tagReducer = (state = initialState, action) => {

}