import {
  FETCHING_USERS,
  USERS_FETCHED,
  FETCHING_USER_POSTS,
  USER_POSTS_FETCHED,
  ADDING_USER,
  USER_ADDED,
  USER_ERROR
} from '../constants/actionTypes';

const initialState = {

}

export const userReducer = (state = initialState, action) => {

}