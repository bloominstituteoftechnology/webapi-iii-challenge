//custom
function capitalizer(req, res, next) {
  // next points to the next middleware/route handler in the queue
  res.send('you need to write some javascript for the capitalizer function')
};

module.exports = capitalizer;