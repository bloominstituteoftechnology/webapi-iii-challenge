const express = require('express');

const server = express();
const PORT = 4100;

//middleware


//route handlers


//listening
server.listen(PORT, ()=> {
    console.log(`Server is alive at ${PORT}`);
})