import React from 'react';


const User = (props) => {
        return(
            <div>
                <div>
                <h1>{props.name}</h1>
                </div>
            </div>
        )
}

export default User;