import React from 'react';


const User = (props) => {
        return(
            <div>
                <div>
                this is a user
                <h1>{props.name}</h1>
                </div>
            </div>
        )
}

export default User;