{
  "name": "node-blog",
  "version": "1.0.0",
  "description": "Lab for building an API using Node.js and Express",
  "private": true,
  "main": "index.js",
  "scripts": {
    "server": "nodemon index.js",
    "build": "yarn --cwd ./friends build",
    "postinstall": "yarn --cwd ./friends install && yarn --cwd ./friends build"
  },
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.16.4",
    "knex": "^0.15.2",
    "sqlite3": "^4.0.3"
  },
  "devDependencies": {
    "nodemon": "^1.18.6"
  }
}
